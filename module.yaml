full-build *default:
  deps:
    - vstk.clusterclient.transport.http
    - vstk.clusterclient
    - vstk.core
    - vstk.hosting.core
    - vstk.airlock.client
    - vstk.graphite.reporter
    - vstk.contrails.api
    - vstk.logging.serilog

  build:
    target: Vstk.AirlockConsumer.sln
    configuration: Release
    tool:
      name: dotnet
